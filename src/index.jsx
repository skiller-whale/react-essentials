import React from "react"
import ReactDOM from "react-dom"
import SiteDesign from "./crittr"

const root = document.getElementById("root")
const content = <h1>React and JSX</h1>

ReactDOM.render(content, root)
